example.property= I AM DEV refresh

# DataSource settings: set here your own configurations for the database
spring.datasource.url =	jdbc:mysql://localhost:3306/ms
spring.datasource.username = root
spring.datasource.password =

spring.cloud.config.profile=dev

#docker: http://eureka-server:8070/eureka/; localhost: http://localhost:8070/eureka/
eureka.client.serviceUrl.defaultZone=http://eureka-server:8070/eureka/

#redis
redis.server=redis
redis.port=6379

# security
spring.security.oauth2.client.provider.keycloak.issuer-uri=http://keycloak:8080/realms/auth-realm

# kafka
spring.kafka.bootstrap-servers=kafka:29092